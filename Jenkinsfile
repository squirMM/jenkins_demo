pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "202206071448"
            }
        }
    }
    post {
        always {
           echo 'pipeline 0 done!!!'
        }
    }
}
